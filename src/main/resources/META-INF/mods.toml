modLoader="javafml" #mandatory
loaderVersion="${loader_version_range}"
license="${mod_license}"

[[mods]] #mandatory
modId="${mod_id}" #mandatory
# The version number of the mod
version="${mod_version}" #mandatory
displayName="${mod_name}" #mandatory
authors="${mod_authors}" #optional
logoFile = "assets/vs_orbit/icon.png"  # 图标文件路径

description='''${mod_description}'''
[[dependencies.${mod_id}]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
modId = "valkyrienskies"
mandatory = true
versionRange = "[2.3.0-beta.5,)"
ordering = "AFTER"
side = "BOTH"

[[dependencies.${mod_id}]]
    modId = "cosmos"
    mandatory = true
    versionRange = "[0.0.6,)"
    ordering = "AFTER"
    side = "BOTH"

[[dependencies.${mod_id}]]
modId = "vsch"
mandatory = true
versionRange = "[1.2.1,)"
ordering = "AFTER"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "geckolib"
mandatory = true
versionRange = "[4.0.0,)"
ordering = "AFTER"
side = "BOTH"